// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
const server = {
    hostname: 'localhost:'
  };
  
  export const environment = {
    production: false,
    name: 'development',
    // baseUrlAuth: `${server.hostname}/sp_user/auth/api/v1`,
    // baseUrlAdmins: `${server.hostname}/sp_academic/academic/api/v1`,
    // baseUrlUsers: `${server.hostname}/sp_user/users/api/v1`,
  };